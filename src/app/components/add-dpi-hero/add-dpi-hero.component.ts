import { Component } from '@angular/core';

/**
 * Composant pour l'affichage de la section (hero) dans la page d'ajout de DPI.
 * Ce composant est conçu pour être utilisé comme une section de présentation 
 * dans l'interface de l'utilisateur.
 */
@Component({
  selector: 'app-add-dpi-hero',  // Sélecteur pour utiliser ce composant dans le template HTML
  imports: [],  // Aucune dépendance supplémentaire n'est importée pour ce composant
  templateUrl: './add-dpi-hero.component.html',  // Template HTML associé au composant
  styleUrl: './add-dpi-hero.component.css'  // Fichier CSS pour styliser le composant
})
export class AddDpiHeroComponent {
  
}

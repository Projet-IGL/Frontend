import { Component } from '@angular/core';

/**
 * Composant représentant le formulaire de mise à jour.
 * Ce composant permet de gérer l'affichage et les interactions liées au formulaire de mise à jour.
 */
@Component({
  selector: 'app-up-form', // Le sélecteur pour utiliser ce composant dans un template
  imports: [], // Modules supplémentaires à importer, si nécessaire
  templateUrl: './up-form.component.html', // Chemin vers le fichier de template HTML
  styleUrl: './up-form.component.css' // Chemin vers le fichier de styles CSS
})
export class UpFormComponent {
  // Ce composant ne contient pas de logique pour l'instant
}
